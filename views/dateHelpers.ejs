<% function toPHFormat(dateString) { if (!dateString) return ''; var date = new Date(dateString); return new Intl.DateTimeFormat('sv-SE', { timeZone: 'Asia/Manila', year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit', hour12: false }).format(date).replace(' ', 'T').slice(0,16); } function formatDisplayDate(dateString) { if (!dateString) return ''; var date = new Date(dateString); return new Intl.DateTimeFormat('en-US', { timeZone: 'Asia/Manila', month:
'2-digit', day: '2-digit', year: 'numeric' }).format(date); } %>
